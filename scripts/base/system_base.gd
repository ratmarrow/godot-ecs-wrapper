class_name System extends Node

# NOTE: This is a holdover from when there was shared logic between systems by default. That is no longer the case.
#       HOWEVER! I will be keeping it here because I am sure SOMEONE is going to want to have shared system logic at some point.
